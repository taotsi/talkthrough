export interface TheEditorProps {
    value?: any;
    mode?: string;
}

export interface EditingAreaProps {
    mode?: string;
    editor: any;
}

export interface EditorToolbarProps {
    mode?: string;
}
